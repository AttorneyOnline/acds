{
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "enum": ["set-opt"]
        },
        "result": {
            "type": "string",
            "enum": ["success", "error"]
        }
    },
    "required": ["id", "result"]
}