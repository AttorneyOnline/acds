{
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "enum": ["opts"]
        },
        "options": {
            "type": "object"
        }
    },
    "required": ["id", "options"]
}