{
    "title": "Opts",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "enum": ["opts"]
        },
        "options": {
            "type": "object"
        }
    },
    "additionalProperties": false,
    "required": ["id", "options"]
}