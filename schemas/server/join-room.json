{
    "title": "JoinRoom",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "enum": ["join-room"]
        },
        "result": {
            "type": "string",
            "enum": ["success", "full", "denied"]
        }
    },
    "additionalProperties": false,
    "required": ["id", "result"]
}