{
  "title": "JoinServer",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "enum": ["join-server"]
    },
    "result": {
      "type": "string",
      "enum": ["success", "full", "password", "banned", "other"]
    },
    "message": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": ["id", "result"]
}
