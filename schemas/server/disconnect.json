{
    "title": "Disconnect",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "enum": ["disconnect"]
        },
        "message": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["id"]
}